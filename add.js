// const menuItems = document.querySelectorAll('.text-menu');
// const line = document.querySelector('.line');

// menuItems.forEach((item) => {
//   item.addEventListener('click', () => {
//     // Удаляем класс "active" у всех пунктов меню
//     menuItems.forEach((menuItem) => menuItem.classList.remove('active'));

//     // Добавляем класс "active" только к выбранному пункту меню
//     item.classList.add('active');

//     // Перемещаем линию к активному пункту меню
//     line.style.transform = `translateY(${item.offsetTop}px)`;
//   });
// });
